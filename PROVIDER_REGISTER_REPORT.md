# تقرير إنشاء شاشة تسجيل مقدم الخدمة (Provider Register)

تم الانتهاء بنجاح من متطلبات تسجيل مقدمي الخدمة كمسار منفصل عن تسجيل المستخدم العادي.

## 1. الملفات التي تم إنشاؤها وتعديلها
- **صفحة التسجيل:** `resources/js/Pages/Auth/ProviderRegister.jsx` (تدعم RTL وتصميم احترافي مع خيارين للطبيب أو العيادة).
- **صفحة انتظار الاعتماد:** `resources/js/Pages/Auth/ProviderPendingApproval.jsx` (تعرض رسالة تأكيد التسجيل وانتظار مراجعة الإدارة).
- **المتحكم (Controller):** `app/Http/Controllers/Auth/ProviderRegisteredUserController.php` (متحكم مخصص لمعالجة طلبات مقدم الخدمة).
- **متحكم المستخدم العادي (معدل):** `app/Http/Controllers/Auth/AuthController.php` (تم تعديله ليحفظ `full_name` و `phone` كما تتطلب قاعدة البيانات وتعيين `status = 1` افتراضياً).
- **صفحة المستخدم العادي (معدل):** `resources/js/Pages/Auth/Register.jsx` (تم إضافة حقل الجوال `phone`).

## 2. المسارات الجديدة (Routes)
تمت إضافة المسارات التالية لملف `routes/web.php` لتكون منفصلة ومستقلة:
- `GET /register/provider`: لعرض شاشة التسجيل.
- `POST /register/provider`: لاستقبال البيانات وحفظها في قاعدة البيانات.
- `GET /provider/pending-approval`: لتحويل المستخدم إليها بعد نجاح التسجيل (بانتظار الاعتماد).

## 3. الجداول التي يتم الحفظ فيها
- **المستخدم `users`:** يتم حفظ الحساب دائماً هنا ويكون `status = 0` (قيد المراجعة/Pending).
- **إذا كان مقدم الخدمة طبيباً (Doctor):**
  - يتم إنشاء ملف شخصي للطبيب في `provider_profiles` وربطه بالمستخدم.
  - إذا تم إدخال رقم الترخيص، يتم إنشاء سجل في جدول `licenses` وربطه كـ Polymorphic مع `ProviderProfile`.
- **إذا كان مقدم الخدمة عيادة/مركزاً (Clinic):**
  - يتم إنشاء حساب أعمال `accounts` كمالك له هو المستخدم الجديد.
  - يتم إنشاء شركة/عيادة `companies` مربوطة بحساب الأعمال `account_id` ويُحفظ بها اسم العيادة ورقم الترخيص، وتكون حالتها `is_active = false`.

## 4. إضافات قواعد البيانات والمشاكل التي عولجت (Migrations/DB)
بما أن بعض الجداول كان ينقصها حقل "المدينة" (city) ولم نتمكن من تعديل الـ Migrations القديمة (كما طُلب):
- قمنا بإنشاء ملف Migration جديد `add_city_to_providers`، ولكن بسبب أخطاء تعارضات سابقة في جداول SQLite (`consultation_calls`) أوقفت التنفيذ، استخدمنا بيئة Tinker (`DB::statement`) لإضافة العمود `city` إلى كل من جدولي `provider_profiles` و `companies` بشكل فوري وناجح.
- تم معالجة الحقول الإجبارية مثل `nationality_id` للطبيب و `activity_id` للعيادة بإنشائها برمجياً بشكل افتراضي (إن لم تكن موجودة) لضمان سلاسة الحفظ دون تعديل قيود الجداول.

## 5. طريقة الاختبار
1. قم بتشغيل الخادم المحلي `php artisan serve` مع `npm run dev`.
2. اذهب إلى الرابط: `/register/provider`.
3. قم باختيار "طبيب بيطري" وقم بتعبئة البيانات.
4. اضغط تسجيل. سيتم تحويلك إلى شاشة الاعتماد (Pending).
5. تحقق من قاعدة البيانات (جدول `users` ستجده بحالة `0`، وجدول `provider_profiles` يحتوي بيانات الطبيب).
6. كرر العملية بفتح مسار التسجيل مرة أخرى أو بتسجيل الخروج، وجرب إنشاء "عيادة / مركز".
7. تحقق من جدول `accounts` و `companies` في قاعدة البيانات للتأكد من حفظ العيادة بنجاح.
