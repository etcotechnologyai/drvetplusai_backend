# تقرير نظام اعتماد ومراجعة مقدمي الخدمة (SaaS)

يوثق هذا التقرير البنية البرمجية المتكاملة لتدفق مراجعة وتدقيق واعتماد طلبات مقدمي الخدمة (العيادات والأطباء البيطريين المستقلين) في لوحة تحكم الإدارة.

---

## 1. الجداول والأعمدة المستخدمة

| الجدول | العمود / الأعمدة | الوظيفة |
| :--- | :--- | :--- |
| `users` | `status` | حالة الحساب: `0` (معلق / بانتظار التدقيق)، `1` (نشط ومعتمد)، `2` (مرفوض)، `3` (موقوف مؤقتاً). |
| `users` | `role` | دور المستخدم. يجب أن يكون `provider` لمقدم الخدمة، و `admin` للمدقق الإداري. |
| `users` | `approved_at` | ختم تاريخ ووقت الموافقة على الطلب وتنشيط الحساب (`datetime`). |
| `users` | `rejection_reason` | نص يوضح سبب رفض الطلب من قبل الإدارة في حال تغيير الحالة إلى `2`. |
| `accounts` | `is_active` | حالة تفعيل الحساب المالي للمنشأة (يصبح `true` عند الاعتماد، و `false` عند الرفض أو التعليق). |
| `companies` | `is_active` | حالة تفعيل المنشأة البيطرية/العيادة التجارية (يصبح `true` عند الاعتماد، و `false` عند الرفض أو التعليق). |
| `provider_profiles` | `is_active` | حالة تفعيل ملف الطبيب البيطري المستقل (يصبح `true` عند الاعتماد، و `false` عند الرفض أو التعليق). |
| `licenses` | `type`, `number`, `meta` | مستندات التراخيص: السجل التجاري (`commercial`) والترخيص الطبي للممارسة (`medical`). يحتوي `meta` على مسار الملف المرفوع. |

---

## 2. المسارات (Routes) المعرفة

تم ربط العمليات بالمسارات البرمجية التالية (محمية بـ `middleware('auth', 'role:admin')`):

*   **عرض الطلبات:** `GET /admin/providers` (عرض وإحصائيات وفلترة الطلبات)
*   **عرض التفاصيل:** `GET /admin/providers/{id}` (التفاصيل والملحقات وعينات التراخيص)
*   **قرار الاعتماد:** `POST /admin/providers/{id}/approve` (اعتماد وتنشيط الحساب والمنشأة)
*   **قرار الرفض:** `POST /admin/providers/{id}/reject` (رفض وتخزين سبب الرفض)
*   **قرار التعليق:** `POST /admin/providers/{id}/suspend` (إيقاف وتجميد مؤقت للحساب والمنشأة)

---

## 3. صفحات واجهة المستخدم (React / Inertia)

تم بناء الواجهات بالتنسيق الجمالي الكامل (Rich Aesthetics) والتصميم المتجاوب الداعم للغة العربية:

### أ. صفحة جدول الطلبات (`Admin/Providers/Index.jsx`)
*   **التبويبات الذكية (Status Tabs):** تدعم التصفية الفورية بين خمس حالات: الكل، بانتظار المراجعة، معتمد ونشط، مرفوض، موقوف.
*   **جدول البيانات الشامل:** يعرض (الاسم والمفوض، البريد الإلكتروني، رقم الجوال، الكيان التجاري، المدينة، الخدمات الطبية المختارة مثل العيادة/الصيدلية/المختبر، تاريخ التقديم، وحالة الطلب بترميز ملون).
*   **صندوق الرفض المنبثق (Rejection Modal):** واجهة مخصصة تظهر عند النقر على "رفض"، تمنع الرفض بدون سبب وتتحقق من طول النص المدخل برمجياً قبل الإرسال.

### ب. صفحة التدقيق والتفاصيل (`Admin/Providers/Show.jsx`)
*   **معاينة فورية للمستندات (Inline Document Preview):** عرض حي لصور التراخيص والسجل التجاري مباشرة داخل الصفحة دون الحاجة للمغادرة إذا كانت صوراً (PNG/JPG) أو ملفات (PDF) تفاعلية.
*   **الروابط المباشرة للتحميل:** توفير أزرار واضحة لتحميل الملفات المرفقة للمراجعة المحلية.
*   **شريط الإجراءات والقرارات التفاعلي:** يتغير تلقائياً حسب حالة الحساب الحالية ليعرض خيارات (الاعتماد، الرفض المسبب، التعليق المؤقت، إعادة التفعيل).

---

## 4. تدفق الاعتماد والرفض والتعليق (Controller Logic)

تتم كافة العمليات الحساسة في `AdminProviderController` مغلفة بـ **Database Transactions** لضمان سلامة وتناسق البيانات:

1.  **الاعتماد (Approve):** يتم تحويل حالة المستخدم لـ `1`، وتحديث تاريخ الاعتماد `approved_at` للوقت الحالي، وتفعيل المنشأة `companies.is_active = true` والحساب `accounts.is_active = true`.
2.  **الرفض (Reject):** يتم التحقق من صحة سبب الرفض (`min:3`)، تحويل حالة المستخدم لـ `2` وتخزين سبب الرفض، وإلغاء تفعيل المنشأة والحساب.
3.  **التعليق (Suspend):** يتم تحويل حالة المستخدم لـ `3` (موقف/معلق)، وإلغاء تفعيل المنشأة والحساب لضمان عدم قدرتهم على تقديم الخدمات بالمنصة.

---

## 5. الاختبارات البرمجية الآلية (Feature Tests)

تم إنشاء وتفعيل ملف اختبار شامل ومؤتمت بالكامل:
`tests/Feature/AdminProviderApprovalTest.php`

يغطي الاختبار الحالات التالية بنجاح بنسبة **100%**:
1.  **test_admin_can_view_dashboard_stats:** التحقق من قدرة المشرف على مشاهدة إحصائيات الطلبات المعلقة في لوحة التحكم.
2.  **test_admin_can_view_providers_list:** فحص إمكانية تحميل قائمة مقدمي الخدمة بنجاح.
3.  **test_admin_can_view_provider_details:** التحقق من فتح صفحة تفاصيل الطلب وتراخيصه.
4.  **test_non_admin_cannot_view_or_actions_providers:** حظر الصلاحيات وتأكيد حماية النظام بإرجاع `403` للمستخدمين العاديين/مقدمي الخدمة عند محاولتهم تصفح لوحة التحكم أو اتخاذ إجراءات الاعتماد.
5.  **test_admin_can_approve_provider:** محاكاة الاعتماد والتأكد من تحول الحالة لـ `1` وتفعيل الكيان التجاري والمالي.
6.  **test_admin_can_reject_provider_with_reason:** محاكاة الرفض وإثبات حفظ نص سبب الرفض وتجميد الكيان.
7.  **test_admin_cannot_reject_without_reason:** اختبار قيود التحقق (Validation) وتأكيد فشل الرفض بدون نص سبب الرفض.
8.  **test_admin_can_suspend_provider:** محاكاة عملية الإيقاف المؤقت وضمان تجميد حساب مقدم الخدمة مباشرة.
